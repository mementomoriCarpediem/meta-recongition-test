import React from 'react';
import { Route, Routes } from 'react-router';

export default (
  <Routes>
    <Route path={'/'} />
    <Route path={'/ready'} />
    <Route path={'/quiz'} />
    <Route path={'/guess'} />
    <Route path={'/submit'} />
    <Route path={'/result'} />
  </Routes>
);
